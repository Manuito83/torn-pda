class Secrets {
  // YATA Configuration
  static const String yataUrl = "https://yata.yt";
  static const String yataUser = ""; // Leave empty if not using proxy auth
  static const String yataPass = ""; // Leave empty if not using proxy auth
  
  // Webview Configuration
  static const String userAgent = "OpenSourceApp/1.0";

  // Native Auth Configuration
  static const String nativeAuthBaseUrl = ""; // Endpoint for recurrent init data
  
  // Stats/Telemetry Configuration
  static const String statsEndpoint = ""; // Endpoint for logging check-ins
}
