import 'package:flutter/material.dart';

class NativeLoginWidget extends StatelessWidget {
  const NativeLoginWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
